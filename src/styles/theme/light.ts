import { Colors } from '../colors';

export default {
  title: 'light',

  colors: {
    borders: Colors.bordersLight,
    texts: Colors.textColorDark,
    postColor: Colors.blackLight,
    highlight: Colors.highlight,

    mediumBackground: Colors.whiteSmoth,
    background: Colors.white,

    white: Colors.white,
    blackLighter: Colors.blackLighter,
  },
} as const;
