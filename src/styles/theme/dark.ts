import { Colors } from '../colors';

export default {
  title: 'dark',

  colors: {
    borders: Colors.borders,
    texts: Colors.textColor,
    postColor: Colors.white,
    highlight: Colors.highlight,

    mediumBackground: Colors.secondary,
    background: Colors.primary,

    white: Colors.white,
    blackLighter: Colors.blackLighter,
  },
} as const;
